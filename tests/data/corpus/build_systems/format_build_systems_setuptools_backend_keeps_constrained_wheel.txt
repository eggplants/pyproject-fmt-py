%%% case build_systems_tests.rs::test_format_build_systems_setuptools_backend_keeps_constrained_wheel
%%% input -
[build-system]
requires = ["setuptools", "wheel>=0.40"]
build-backend = "setuptools.build_meta"
%%% output short
[build-system]
build-backend = "setuptools.build_meta"
requires = [ "setuptools", "wheel>=0.40" ]
%%% output long
[build-system]
build-backend = "setuptools.build_meta"
requires = [ "setuptools", "wheel>=0.40" ]
%%% output keep-full-version
[build-system]
build-backend = "setuptools.build_meta"
requires = [ "setuptools", "wheel>=0.40" ]
