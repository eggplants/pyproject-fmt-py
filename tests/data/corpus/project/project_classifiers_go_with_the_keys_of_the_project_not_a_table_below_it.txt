%%% case project_tests.rs::test_project_classifiers_go_with_the_keys_of_the_project_not_a_table_below_it
%%% input -
project.name = "test"
project.requires-python = ">=3.12"

[project.urls]
homepage = "https://example.com"
%%% output short
project.name = "test"
project.requires-python = ">=3.12"

[project.urls]
homepage = "https://example.com"
%%% output long
project.name = "test"
project.requires-python = ">=3.12"

[project.urls]
homepage = "https://example.com"
%%% output keep-full-version
project.name = "test"
project.requires-python = ">=3.12"

[project.urls]
homepage = "https://example.com"
%%% output classifiers
project.name = "test"
project.requires-python = ">=3.12"
project.classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: 3.15",
]

[project.urls]
homepage = "https://example.com"
